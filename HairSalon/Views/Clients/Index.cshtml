@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Client>;

<h1>Client List</h1>
<h3>Clients are listed by name and stylist. Click for more details.</h3>

@if (@Model.Count == 0)
{
  <h3>No clients have been added yet!</h3>
  <p>@Html.ActionLink("Add a client", "Create")</p>
} 

<ul>
  @foreach(Client client in Model)
  {
    <li>@Html.ActionLink($"{client.ClientName}", "Details", new { id = client.ClientId }) | @client.Stylist.StylistName</li>
  }
</ul>
<hr />

<p>@Html.ActionLink("Add a client", "Create")</p>
<p>@Html.ActionLink("Return to Homepage", "Index", "Home")</p>